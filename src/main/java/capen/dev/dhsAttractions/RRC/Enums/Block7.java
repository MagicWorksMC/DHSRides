package capen.dev.dhsAttractions.RRC.Enums;

public enum Block7 {
    //Block7 (clear, holding, occupied, unlocked)
    clear("clear", 0),
    occupied("occupied", 0),
    dispatch("dispatch", 0),
    restraints("restraints", 0),
    holding("holding", 0);

    private Block7(final String name, final int ordinal) {

    }
}
