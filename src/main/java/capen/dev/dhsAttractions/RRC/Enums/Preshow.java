package capen.dev.dhsAttractions.RRC.Enums;

public enum Preshow {
    idle("idle", 0),
    countdown("countdown", 0),
    active("active", 0),
    locked("locked", 0);

    private Preshow(final String name, final int ordinal) {

    }
}

