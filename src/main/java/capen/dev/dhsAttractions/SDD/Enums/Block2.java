package capen.dev.dhsAttractions.SDD.Enums;

public enum Block2 {
    //Block2 (clear, holding, launch, occupied)
    clear("clear", 0),
    occupied("occupied", 0),
    holding("holding", 0);

    private Block2(final String name, final int ordinal) {

    }
}

