package capen.dev.dhsAttractions.TOT.Enums;

public enum Global {
    //Global (automatic, manual, waiting, closed)

    automatic("automatic", 0),
    manual("manual", 0),
    closed("closed", 0),
    waiting("waiting", 0);

    private Global(final String name, final int ordinal) {

    }
}